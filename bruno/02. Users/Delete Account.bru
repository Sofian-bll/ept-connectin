meta {
  name: Delete Account
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/users/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "delete_content": true
  }
}
