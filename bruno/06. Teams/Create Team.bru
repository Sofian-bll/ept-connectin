meta {
  name: Create Team
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/teams
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Team Frontend"
  }
}
