meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/login
  body: json
  auth: none
}

body:json {
  {
    "email": "sofian@epitech.eu",
    "password": "password"
  }
}

script:post-response {
  if (res.getStatus() === 200) {
    bru.setVar("token", res.body.token);
  }
}
